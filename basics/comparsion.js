console.log("2">1) // will convert "2" into integer internally to compare the two entities
console.log("2"===2) // This will show false because "2" is a string and === operator first check data type and then compare it with the given value.

